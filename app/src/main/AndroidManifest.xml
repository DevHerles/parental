<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de Sistema y Red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- Permiso especial para monitoreo de tiempo de pantalla -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".ParentalApp"
        android:allowBackup="false"
        android:dataExtractionRules="@null"
        android:fullBackupContent="false"
        android:icon="@android:drawable/ic_lock_lock"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_lock_lock"
        android:supportsRtl="true"
        android:theme="@style/Theme.AegisParentalControl"
        tools:targetApi="34">

        <!-- Actividad Principal (Compose) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:resizeableActivity="false"
            android:supportsPictureInPicture="false"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AegisParentalControl"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de Bloqueo a pantalla completa (LockScreenActivity) -->
        <activity
            android:name=".ui.child.LockScreenActivity"
            android:excludeFromRecents="true"
            android:resizeableActivity="false"
            android:supportsPictureInPicture="false"
            android:exported="false"
            android:launchMode="singleTop"
            android:noHistory="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AegisParentalControl" />

        <!-- Receptor de Administrador de Dispositivo (Anti-desinstalación) -->
        <receiver
            android:name=".core.security.ParentalDeviceAdminReceiver"
            android:description="@string/device_admin_description"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <!-- Servicio de Accesibilidad (Centinela en tiempo real) -->
        <service
            android:name=".service.ParentalAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Receptor de Arranque para persistencia tras reinicio -->
        <receiver
            android:name=".service.BootReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.parental.control.ACTION_CLEAR_PAUSE" />
                <action android:name="com.parental.control.ACTION_LOCK_NOW" />
                <action android:name="com.parental.control.ACTION_TEMPORARY_UNLOCK" />
                <action android:name="com.parental.control.ACTION_SET_BEDTIME_ENABLED" />
                <action android:name="com.parental.control.ACTION_SET_PIN" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
